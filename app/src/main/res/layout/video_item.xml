<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginBottom="12dp"
    android:focusable="false">

    <!-- Контентная область: фокус = выбрать видео -->
    <LinearLayout
        android:id="@+id/cardContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_card"
        android:stateListAnimator="@animator/card_focus"
        android:focusable="true"
        android:clickable="true"
        android:padding="12dp">

        <FrameLayout
            android:layout_width="160dp"
            android:layout_height="90dp"
            android:layout_marginEnd="16dp">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/thumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="@color/surface_variant"
                app:shapeAppearanceOverlay="@style/ThumbShape" />

            <ImageView
                android:id="@+id/selectedMark"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="top|end"
                android:layout_margin="6dp"
                android:src="@android:drawable/checkbox_on_background"
                android:tint="@color/success"
                android:visibility="invisible"
                android:contentDescription="@string/selected_mark" />
        </FrameLayout>

        <TextView
            android:id="@+id/videoTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/on_surface"
            android:textSize="18sp"
            android:maxLines="2"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Кнопка удаления: отдельный фокусируемый элемент, D-pad вправо с карточки -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/deleteButton"
        style="@style/Widget.Material3.Button.IconButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:contentDescription="@string/delete_button"
        app:icon="@android:drawable/ic_menu_delete"
        app:iconTint="@color/on_surface_muted" />

</LinearLayout>
